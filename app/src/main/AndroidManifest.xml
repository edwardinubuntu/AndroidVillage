<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.soleil.androidvillage" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/fish_hook_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:logo="@drawable/ic_action_lab"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

	    <activity android:name=".activity.ControllingExecutionActivity"
	              android:label="@string/chapter_controlling_execution"
	              android:logo="@drawable/ic_action_lab" />

    </application>

</manifest>
